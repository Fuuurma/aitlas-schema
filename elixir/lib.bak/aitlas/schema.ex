defmodule Aitlas.Schema do
  @moduledoc """
  Shared Ecto schemas for Aitlas projects.
  
  Usage in your Elixir project:
  
      def deps do
        [
          {:aitlas_schema, path: "path/to/aitlas-schema/elixir"}
        ]
      end
  
  Then import the schemas you need:
  
      alias Aitlas.Schema.{User, Session, Account}
  """
end