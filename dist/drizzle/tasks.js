// Tasks
import { pgTable, text, timestamp, integer, uuid, json, } from "drizzle-orm/pg-core";
import { users } from "./users";
export const tasks = pgTable("tasks", {
    id: uuid("id").primaryKey(),
    userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
    agentSlug: text("agent_slug").notNull(),
    agentSpec: json("agent_spec").$type(),
    goal: text("goal").notNull(),
    context: text("context"),
    provider: text("provider").notNull(),
    model: text("model"),
    seed: integer("seed"),
    creditBudget: integer("credit_budget"),
    maxIterations: integer("max_iterations").default(20),
    maxToolCalls: integer("max_tool_calls").default(50),
    maxTokens: integer("max_tokens").default(200000),
    maxRuntimeMs: integer("max_runtime_ms").default(1800000),
    status: text("status").notNull().default("pending"),
    iteration: integer("iteration").default(0),
    toolCallsMade: integer("tool_calls_made").default(0),
    tokensUsed: integer("tokens_used").default(0),
    creditsUsed: integer("credits_used").default(0),
    creditsReserved: integer("credits_reserved").default(0),
    result: text("result"),
    error: text("error"),
    startedAt: timestamp("started_at"),
    completedAt: timestamp("completed_at"),
    executionHash: text("execution_hash"),
    parentTaskId: uuid("parent_task_id"),
    rootTaskId: uuid("root_task_id"),
    graphDepth: integer("graph_depth").default(0),
    createdAt: timestamp("created_at").notNull().defaultNow(),
    updatedAt: timestamp("updated_at").notNull().defaultNow(),
    retryCount: integer("retry_count").notNull().default(0),
    workerId: text("worker_id"),
    heartbeatAt: timestamp("heartbeat_at"),
    replayOfTaskId: uuid("replay_of_task_id"),
    forkFromStep: integer("fork_from_step"),
});
export const taskSteps = pgTable("task_steps", {
    id: uuid("id").primaryKey(),
    taskId: uuid("task_id").notNull().references(() => tasks.id, { onDelete: "cascade" }),
    stepNumber: integer("step_number").notNull(),
    action: text("action").notNull(),
    input: json("input").$type(),
    output: json("output").$type(),
    metadata: json("metadata").$type().default({}),
    createdAt: timestamp("created_at").notNull().defaultNow(),
});
export const toolCalls = pgTable("tool_calls", {
    id: uuid("id").primaryKey(),
    taskId: uuid("task_id").notNull().references(() => tasks.id, { onDelete: "cascade" }),
    stepId: uuid("step_id"),
    toolName: text("tool_name").notNull(),
    arguments: json("arguments").$type().notNull(),
    result: json("result").$type(),
    error: text("error"),
    childTaskId: uuid("child_task_id"),
    createdAt: timestamp("created_at").notNull().defaultNow(),
});
export const executionLogs = pgTable("execution_logs", {
    id: text("id").primaryKey(),
    taskId: uuid("task_id").references(() => tasks.id, { onDelete: "cascade" }),
    agentId: text("agent_id"),
    level: text("level").notNull(),
    message: text("message").notNull(),
    metadata: json("metadata").$type().default({}),
    createdAt: timestamp("created_at").notNull().defaultNow(),
});
//# sourceMappingURL=tasks.js.map